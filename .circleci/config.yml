version: 2.1

jobs:

  test:
    docker:
      - image: circleci/android:api-30-node
    resource_class: medium

    steps:
      - checkout
      - run:
          name: Check gcloud
          command: gcloud --version
            
workflows:
  push:
    jobs:
      - test
