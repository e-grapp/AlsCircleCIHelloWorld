version: 2.1

parameters:
  run_workflow:
    type: boolean
    default: false
  run-setup:
    type: boolean
    default: false

jobs:
  build:
    docker:
      - image: cimg/base:2023.07

    steps:
      - run:
          name: PWD
          command: pwd

# workflows:
#   build_and_deploy:
#     jobs:
#       - build
          # type: approval
          # matrix:
          #   parameters:
          #     1-array: [0,1]
          #     2-array: [0,1]
          #     3-array: [0,1]
          #     4-array: [0,1]

workflows:
  workflow:
    when:
      and:
        - equal: [true, << pipeline.parameters.run-setup >>]
        - equal: [true, << pipeline.parameters.run_workflow >>]
    jobs:
      - build