version: 2
jobs:
  build:
    macos:
      xcode: 12.5.0
    resource_class: m2.medium
    steps:
      - checkout
      - run:
          name: Print "Hello World"
          command: |
             echo "Hello World"
workflows:
  version: 2
  build:
    jobs:
      - build
