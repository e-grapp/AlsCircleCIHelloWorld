version: 2.1
  
jobs:
  cimg:
    docker:
      - image: cimg/ruby:2.6.6-node
    steps:
      - checkout
      - store_test_results:
          path: bazel-testlogs

workflows:
  build-deploy:
    jobs:
      - cimg