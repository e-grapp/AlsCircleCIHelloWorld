version: 2
jobs:
  build:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - run:
          name: Print "Hello World"
          command: |
             echo "Hello World"
workflows:
  version: 2
  build:
    jobs:
      - build
