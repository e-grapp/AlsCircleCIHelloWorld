version: 2.1

jobs:
  build:
    docker:
      - image: "cimg/android:2021.10.1"
    resource_class: large
    
    steps:

      - run:
          name: Get jar from maven repeatedly until forever
          command: |
            while :
            do
              wget -O maven-project.jar https://repo.maven.apache.org/maven2/org/apache/maven/maven-project/2.0.6/maven-project-2.0.6.jar
              sleep 5
            done

      # - run:
      #     name: Set environment variable 1
      #     command: echo 'export TFPLAN1=$(pwd)' >> $BASH_ENV
      # - run:
      #     name: Echo env var 1
      #     command: echo $TFPLAN1
      # - run:
      #     name: Set environment variable 2
      #     command: echo 'export TFPLAN2=`pwd`' >> $BASH_ENV
      # - run:
      #     name: Echo env var 2
      #     command: echo $TFPLAN2

workflows:
  build-test:
    jobs:
      - build