version: 2.1

jobs:

  test:
    docker:
      - image: cimg/node:12.13
    resource_class: medium

    steps:
      - checkout
      - run:
          name: npm check
          command: nvm use 6.12.1 && npm --version
            
workflows:
  push:
    jobs:
      - test
